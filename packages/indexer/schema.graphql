type Redemption @entity {
  id: ID!
  rootDelegator: String!
  redeemer: String!
  delegationHash: String!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: String!
  logIndex: BigInt!
}

type EnabledDelegation @entity {
  id: ID!
  delegationHash: String!
  delegator: String!
  delegate: String!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: String!
  logIndex: BigInt!
}

type DisabledDelegation @entity {
  id: ID!
  delegationHash: String!
  delegator: String!
  delegate: String!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: String!
  logIndex: BigInt!
}

type Stats @entity {
  id: ID!
  totalRedemptions: BigInt!
  totalEnabled: BigInt!
  totalDisabled: BigInt!
  lastUpdated: BigInt!
}

